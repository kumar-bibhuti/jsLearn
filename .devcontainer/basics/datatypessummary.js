// Primitive

// 7 types : String, Number, Boolean, null, undefined, symbol, Bigint

const data = ["ram", "mohan", "shyam"]
console.log(typeof data);

let myobject = {
    "id" : 1,
    "name" : "Bibhuti",
}
console.log(typeof myobject);

// Refrence (Non primitive)

// Array, Object, Funcitons